import { Bounded } from '@seoul-rebels/ui';
import React from 'react';

const StockistPage = async (): Promise<React.JSX.Element> => {
  return <Bounded>StockistPage</Bounded>;
};

export default StockistPage;
